# SQLite Database Configuration - Dynamic path set by Electron
DATABASE_URL="file:./medlab.db"
NODE_ENV=production

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Server Configuration  
PORT=3001
HOST=localhost

# CORS Configuration - Allow both file:// and localhost for Electron
FRONTEND_URL=file://
CORS_ORIGIN=file://,http://localhost:5173,http://localhost:3000
